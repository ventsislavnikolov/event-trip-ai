# EventTrip canonical environment variables
# Do not commit real secret values.

# Core runtime (required in dev/preview/prod)
# Generate: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****
# Supabase Postgres connection string (Project Settings -> Database -> Connection string)
# Use pooled connection URL in production.
POSTGRES_URL=****

# AI model access
# For non-Vercel/self-hosted runtime, provide AI Gateway key.
# For Vercel runtime, OIDC can be used instead.
AI_GATEWAY_API_KEY=****

# Optional platform integrations
# Vercel Blob token (required only when file upload/artifact flows are enabled)
BLOB_READ_WRITE_TOKEN=****
# Redis stream resume support (optional for MVP baseline)
REDIS_URL=****

# Optional intent parsing model routing
EVENTTRIP_INTENT_PRIMARY_MODEL=
EVENTTRIP_INTENT_FALLBACK_MODEL=

# Event provider API keys (required when provider integrations are enabled)
TICKETMASTER_API_KEY=
SEATGEEK_CLIENT_ID=
SEATGEEK_CLIENT_SECRET=
TRAVELPAYOUTS_API_TOKEN=
TRAVELPAYOUTS_MARKER=
# Optional runtime airport aliases for city->IATA normalization.
# Supported formats:
# 1) JSON: {"plovdiv":"PDV","varna":"VAR"}
# 2) CSV: plovdiv=PDV; varna=VAR
EVENTTRIP_AIRPORT_ALIAS_MAP=
